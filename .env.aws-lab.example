# .env.aws-lab.example
# AWS Lab Environment Configuration Template
#
# Instructions:
#   1. Copy this file: cp .env.aws-lab.example .env.aws-lab
#   2. Fill in your AWS credentials from the lab console
#   3. The .env.aws-lab file is gitignored and will not be committed
#
# Variable Priority: CLI arguments > Environment variables > Defaults

# ============================================================================
# AWS CREDENTIALS (Required)
# ============================================================================
# Get these from AWS Academy Lab Details or IAM console
# WARNING: Never commit real credentials to version control
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_SESSION_TOKEN=your_session_token_here

# ============================================================================
# AWS REGION AND STACK CONFIGURATION
# ============================================================================
AWS_REGION=us-west-2
STACK_NAME=ha-web-platform
ENVIRONMENT=dev
RESOURCE_PREFIX=infra-ha-web-dev

# ============================================================================
# EC2 AND AUTO SCALING GROUP
# ============================================================================
INSTANCE_TYPE=t2.micro
DESIRED_CAPACITY=2
MIN_SIZE=2
MAX_SIZE=2

# AMI: Amazon Linux 2 for us-west-2 (region-specific)
# Find latest: aws ec2 describe-images --owners amazon \
#   --filters "Name=name,Values=amzn2-ami-hvm-*-x86_64-gp2" \
#   --query 'sort_by(Images,&CreationDate)[-1].ImageId' --output text
AMI_ID=ami-022bee044edfca8f1

# ============================================================================
# NETWORK CONFIGURATION (VPC AND SUBNETS)
# ============================================================================
VPC_CIDR=10.0.0.0/16
PUBLIC_SUBNET_1_CIDR=10.0.1.0/24
PUBLIC_SUBNET_2_CIDR=10.0.2.0/24
PRIVATE_SUBNET_1_CIDR=10.0.11.0/24
PRIVATE_SUBNET_2_CIDR=10.0.12.0/24

# ============================================================================
# OPTIONAL FEATURES
# ============================================================================
# VPC Endpoint for DynamoDB (private access without internet)
# Set to 'no' if your lab role restricts endpoint creation
CREATE_DDB_VPC_ENDPOINT=yes
